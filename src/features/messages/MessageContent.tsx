import React from "react";

export function MessageContent() {
    // TODO : check if array or not
    // const { messageContent, messageType } = message;
    // const isSingle = messageType === "single";
    return (
        <div>
            {/* {isSingle ? (
                <div>{messageContent} </div>
            ) : (
                <>
                    {(messageContent as MessageContentObject[]).map(({ source, amount }) => {
                        {
                            source;
                        }
                        {
                            amount;
                        }
                    })}
                </>
            )} */}
        </div>
    );
}
